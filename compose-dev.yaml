services:
  agencia:
    build: .
    image: austin-dev # simply names the image that is being built
    container_name: austin-dev-container
    # volumes: .:/agencia



# services:
#   app:
#     entrypoint:
#     - sleep
#     - infinity
#     image: docker/dev-environments-default:stable-1
#     init: true
#     volumes:
#     - type: bind
#       source: /var/run/docker.sock
#       target: /var/run/docker.sock

